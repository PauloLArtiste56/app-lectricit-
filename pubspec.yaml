name: elecapp
description: "Appli d'apprentissage des bases de l'électricité, puis préparation à l'habilitation NF C 18-510."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # Contenu pédagogique embarqué (lecture seule) et images des fiches.
  assets:
    - assets/content.json
    - assets/images/
